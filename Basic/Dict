# 取出 key ,value
# for循环取索引和值, 使用iteritems
d = {'a': 1, 'b': 2, 'c': '3'}
for k, v in d.items():
   print( '{}: {}'.format(k, v) )        # k为key, v为value值

# 新增或更新資料
d[key] = value

# 刪除資料 - 刪除一筆元素的key - value
del d['key']

# 印出字典裡的所有 key
d.keys()
d.values()   # 印出所有的 value
d.items()    # 印出所有的元素


