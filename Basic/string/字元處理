# 字元處理

A = "012345"

A[:1]    # 從頭開始，取到index 1 ( 不包含)
⇒ 0

A[2:3]   # 只取 index 2 的 Item
⇒ 2

A[3:6]   # 從index 3 開始，取到index 6 ( 不包含)
⇒ 345  
          # 個數等於  6 - 3 = 取3個 Item

A[4:]     # 由 index 4 ，一直取到結尾
⇒ 45
